# {{ ansible_managed }}

# How long to wait for successfull mysql startup
# Startup can take a while if it requires a galera state transfer.
MYSQLD_STARTUP_TIMEOUT=1800

ulimit -n  {{ galera_max_connections }}
ulimit -Hn  {{ galera_max_connections }}

