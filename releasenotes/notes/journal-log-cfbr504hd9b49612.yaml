---
deprecations:
  - The log path, ``/var/log/cloudkitty`` is no longer used to capture service
    logs. All logging for the cloudkitty service will now be sent directly to the
    systemd journal.
