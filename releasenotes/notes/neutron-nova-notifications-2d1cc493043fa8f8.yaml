---
upgrade:
  - Neutron notifications to nova will now use the internal API
    endpoint instead of the public endpoint.
other:
  - Neutron notifications to nova will now use the internal API
    endpoint instead of the public endpoint.
