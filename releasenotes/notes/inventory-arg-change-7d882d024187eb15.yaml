---
upgrade:
  - The ``dynamic_inventory.py`` script now takes a
    ``--config`` argument rather than a ``--file``
    argument.
deprecations:
  - The ``dynamic_inventory.py`` script now takes a
    ``--config`` argument rather than a ``--file``
    argument.
