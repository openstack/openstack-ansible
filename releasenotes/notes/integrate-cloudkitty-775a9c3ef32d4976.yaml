---
features:
  - Support has been added to allow the deployment of the
    Cloudkitty service when hosts are present in the host group
    `cloudkitty_hosts` in openstack_user_config or conf.d files.
    `os-cloudkitty-install.yml` has been added and is now part of
    `setup-openstack.yml`
