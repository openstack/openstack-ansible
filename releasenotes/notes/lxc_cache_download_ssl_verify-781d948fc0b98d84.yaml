---
features:
  - TLS certificate chain verification during the download of
    LXC cached images can now be toggled using the configuration
    variable 'lxc_cache_validate_certs'. The default behavior is
    to validate the certificate chain.
