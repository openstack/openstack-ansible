---
features:
  - |
    Added variables ``masakari_monitor_corosync_multicast_ports`` and
    ``masakari_monitor_corosync_ipmi_check`` that allow to define ports used
    by corosync service and to enable IPMI checks in case ipmi RA is set in
    pacemaker.
