---

features:
  - |
    Implemented variables ``rally_openstack_git_repo`` and
    ``rally_openstack_git_install_branch`` that allow to override installation
    source for rally-openstack package as well as controll installed version
    of the package.
