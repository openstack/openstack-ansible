---
issues:
  - |
    Octavia coordination and jobboard are disabled due to ZooKeeper instability
    (ConnectionLoss and session expiration issues) until the problem is resolved.
    Redis can be used as an alternative backend if coordination is required.
